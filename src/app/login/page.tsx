import LoginClient from "@/components/login";

interface Props {
    
}

const Login: React.FC<Props> = ({  }) => {
    return (
        <>
            <LoginClient />
        </>
    );
};

export default Login;